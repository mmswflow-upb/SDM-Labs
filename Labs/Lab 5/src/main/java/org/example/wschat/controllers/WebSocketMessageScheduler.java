package org.example.wschat.controllers;

import org.springframework.stereotype.Component;

@Component
public class WebSocketMessageScheduler {
    // No longer sending periodic chat messages as we now have AuctionScheduler
}
